import React, { Component } from 'react';
import { connect } from 'react-redux';
import { Container} from 'semantic-ui-react'

class ExploreBlockchain extends Component {
    render() {
        return (<Container>
            
        </Container>
        );
    }
}

export default connect(null, null)(ExploreBlockchain);